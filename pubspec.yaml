name: flutter_japanese_address_picker
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.2.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  freezed: ^2.3.3 
  build_runner: ^2.4.2
  json_serializable: ^6.6.2

flutter:
  assets:
    - assets/address_seeds.json